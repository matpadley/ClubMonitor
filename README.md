# ClubMonitor

This is a .NET 10 solution for DDD structure with a Blazor Web App, Minimal API, and EF Core with PostgreSQL.

## Build and Run Instructions

1. Restore the packages using `dotnet restore`
2. Build the solution using `dotnet build`
3. Run the application using `dotnet run` in the appropriate project directory.